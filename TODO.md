# План переработки Telegram бота в Telegram Web App (Mini App)

## Выполненные задачи (Архив)
- [x] **Завершенные фичи:** Настроен shell, терминал, запуск конфигураций, просмотр, скачивание и удаление артефактов через UI, метрики, inference с настройками, экспорт в HuggingFace, ролевая модель доступа, загрузка файлов артефактов через UI.

## Текущие и будущие задачи

### Шаг 11: Управление датасетами
 - [x] **Dataset Management UI (Шаг 11: Управление датасетами):**
  - Создать директорию `data/`, если она не существует.
  - Добавить эндпоинт `/api/datasets` для получения списка доступных датасетов в `data/`.
  - Добавить эндпоинт `/api/upload_dataset` для загрузки файлов датасетов (`.txt`, `.json` и т.д.) в директорию `data/`.
  - Добавить раздел на фронтенде для просмотра и загрузки файлов датасетов.
